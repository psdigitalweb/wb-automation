# Example htpasswd file for Adminer basic auth
# 
# To generate a real password file, run on the server:
#   apt-get update && apt-get install -y apache2-utils
#   htpasswd -c nginx/.htpasswd admin
#
# This will prompt for a password and create the file.
# Then restart nginx:
#   docker compose restart nginx
#
# Example entry format (DO NOT USE THIS - it's a dummy hash):
# admin:$apr1$dummy$hashhere123456789

