# Logic Status Matrix

| Capability | Intended behavior | Implemented status | Evidence | Gaps / caveats |
|---|---|---:|---|---|
| Query ingestion | Import WB/search query CSVs into project/category corpus with raw and normalized rows. | Implemented | `src/app/services/seo/query_pipeline/ingestion.py::import_queries_from_csv`; `src/app/routers/seo_query_import.py::import_seo_query_csv_endpoint`; models `SeoQueryBatch`, `SeoQueryRaw`, `SeoQueryNormalized`. | Failure-state semantics need close read; unique constraints are per batch, not global corpus. |
| Normalization | Normalize text for matching/deduping and parse query/frequency columns. | Implemented | `src/app/services/seo/query_pipeline/normalization.py::normalize_query_text`, `resolve_query_column`, `resolve_frequency_column`, `extract_query_text`, `extract_frequency`. | Normalization is deterministic lexical; no morphology/stemming layer evident in import normalization. |
| Unified query dataset | Combine CSV and WB search-report sources into canonical rows. | Implemented | `src/app/services/seo/query_pipeline/unified_dataset.py::assemble_unified_query_dataset`; dataclasses `CanonicalQueryRow`, `CanonicalQuerySourceRef`. | Depends on `products` joins to scope WB report rows; hidden dependency on search report schema. |
| Lexical/hybrid grouping | Persist lexical clusters and inherit/override annotations by cluster. | Implemented | `query_pipeline/clustering.py::run_query_clustering`; `query_pipeline/hybrid.py::run_query_hybrid_annotation`; models `SeoQueryCluster`, `SeoQueryClusterMembership`, `SeoQueryAnnotationVersion`. | Query clustering is lexical/deterministic, not final semantic clustering. Hybrid projection is stored in annotation meta rather than its own table. |
| Clustering | Cluster queries and eventually SKU/product representations. | Partially implemented | Query clustering implemented in `query_pipeline/clustering.py`; experimental semantic comparison in `query_pipeline/semantic.py`; SKU clustering placeholder in `services/seo/clustering/service.py::cluster_skus_placeholder`. | SKU clustering is placeholder/legacy. Query semantic clustering is experiment/debug, not default pipeline. |
| Annotations | Store pruning/basic/hybrid annotation decisions with versioning. | Implemented | `query_pipeline/pruning.py::_persist_annotations`, `run_query_pruning_and_basic_annotation`; `query_pipeline/hybrid.py`; models `SeoQueryAnnotation`, `SeoQueryAnnotationVersion`. | Annotation meta is overloaded for hybrid projection. |
| Profile extraction | Extract cluster-level markers for product type/use case/attributes/language. | Partially implemented | `query_pipeline/profiles.py::run_query_profile_extraction`; diagnostics `ExtractedClusterProfile`. | Profiles are returned as diagnostics/projection only; `SeoClusterProfile` tables are not wired by current profile service. |
| Category meaning | Build product-side category meaning and axes. | Partially implemented | MVP deterministic builder `meaning_extraction/category_meaning.py::build_category_meaning`; persistent axes via `category_bootstrap.py::_upsert_axes`, `SeoCategoryMeaningAxes`. | Two category-meaning concepts coexist: deterministic dataclass `CategoryMeaning` and persisted LLM/deterministic axes payload. Expressive cache read is optional and silent on failure. |
| SKU meaning | Build SKU evidence, draft meaning, save annotation, produce atoms/vision atoms. | Implemented, with fallback | `sku_meaning/evidence.py::build_sku_evidence_pack`; `sku_meaning/draft.py::generate_sku_meaning_draft`; `sku_meaning/annotations.py::save_annotation`; `products.py::run_product_analysis`; `meaning_atoms/storage.py::ensure_sku_atoms`. | Draft LLM failure falls back to product-data payload in `products.py::run_product_analysis`; quality depends on provider/cache/vision availability. |
| Query meaning | Build persistent query meaning library from query clusters/profiles, optionally LLM. | Implemented | `query_meaning_matcher/library.py::build_query_meaning_library`, `list_query_meanings`; model `SeoQueryMeaning`. | Default is deterministic (`use_llm=False`); docs phase says LLM query meaning is intended. |
| Atoms | Use structured atom representations for eligibility-first matching. | Partially implemented | `meaning_atoms/storage.py::build_query_atoms_for_category`, `ensure_sku_atoms`, `get_atoms_payload`; experiment `experiments/meaning_atoms/v1.py::match_atoms_v1`; model `SeoMeaningAtom`. | Docs `23_atoms_v1...` says production preview/shadow and separate storage/matcher runs; code uses generic atom table and matcher output in query sets. |
| Scoring | Score query/SKU fit with explainable components. | Partially implemented | Diagnostics scoring: `scoring/preparation.py::run_query_scoring_preparation`, `scoring/actual.py::run_query_actual_scoring`; persistence helper `scoring/service.py::persist_query_score`. | Actual scoring does not appear wired to score persistence tables. Generation has separate SEO relevance scoring in `generation/service.py`. |
| Content generation | Generate WB title/characteristics/description from selected queries and product facts using two-model policy. | Implemented as draft generation | `generation/service.py::run_seo_generation`; prompt `generation/prompts/wb_card_system_v1.md`; router `seo_generation.py`; schemas `seo_generation.py`. | Newer implementation contradicts older docs saying not to implement generation yet. No WB publish path. Frontend brief preview hardcodes model names while backend settings are configurable. |
| Explainability | Every decision should be explainable and versioned. | Partially implemented | Query annotation versions; matcher reasons in `SeoSkuQuerySetItem.reasons_payload`; generation validation/relevance reports in `SeoContentVersion.score_breakdown`; docs `00_master_context.md`. | No unified trace table. Score explanations table exists but current scoring path does not write it. |
| Observability | Diagnostics/debug surfaces for pipeline status and failures. | Partially implemented | Debug router/page `seo_query_pipeline_debug.py`, frontend `query-pipeline/debug/page.tsx`; bootstrap status `seo_category_bootstrap.py`; generation latest/recalc endpoints. | Observability is mostly ad hoc API diagnostics and payloads, not structured metrics/logging. |
| Review integration | Use reviews for category expressive meaning and SKU evidence. | Implemented/partial | `expressive_llm/reviews_source.py::fetch_category_review_scope`; `sku_meaning/evidence.py::_fetch_sku_reviews`; `category_meaning.py::_load_llm_expressive_from_cache`; `category_bootstrap.py::_fetch_review_evidence`. | Review usage is split: offline category expressive cache, category bootstrap evidence, SKU evidence. Product projection MVP explicitly has no reviews dependency. |
| Product attribute integration | Use product fields/attributes/photos as core evidence. | Implemented | `meaning_extraction/category_meaning.py`, `product_projection.py`; `sku_meaning/evidence.py`; `products.py`; `generation/service.py::_build_generation_brief`. | Many accesses use raw SQL/text and JSON-ish flattening; schema drift risk. |
| LLM integration boundary | Call LLM through provider interfaces and settings, cache expensive calls where needed. | Implemented/partial | `providers/base.py::ChatProvider`, `EmbeddingProvider`; `providers/openrouter.py::OpenRouterProvider`; `settings.py` OpenRouter/generation settings; caches in `expressive_llm/storage.py`, `sku_meaning/draft.py`, `category_bootstrap.py`, `query_meaning_matcher/library.py`. | Some paths default to local deterministic provider to avoid network. `ensure_meaning_embedding` defaults to `OpenRouterProvider` when no provider passed, but matcher passes `LocalPreviewEmbeddingProvider`. |
| Frontend SEO workflow | Provide pages for categories, products, SKU analysis, query selection, generation. | Implemented in working tree | `frontend/app/app/project/[projectId]/seo/**`; `frontend/lib/apiClient.ts`. | Entire SEO frontend folder is untracked in current `git status`. |

