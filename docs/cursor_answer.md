### Обновление админского управления тарифами WB

- На бэкенде admin‑эндпоинт `POST /api/v1/admin/marketplaces/wildberries/tariffs/ingest` теперь дополнительно возвращает `task_id` (Celery `result.id`), а агрегатор `/api/v1/admin/marketplaces/wildberries/tariffs/status` для `commission` фильтрует locale по умолчанию на `'ru'` и явно возвращает её в ответе; права по‑прежнему обеспечиваются через `get_current_superuser`.  
- На фронтенде страница `/app/admin/marketplaces/wildberries/tariffs` перед запросами `/status` сначала вызывает `/v1/auth/me` и только при `is_superuser=true` дергает admin‑API, а кнопка “Обновить тарифы WB” блокируется во время запроса и ещё на 10 секунд после старта (debounce); аналогичная admin‑секция “🔒 Admin (глобально, для всех проектов) / Wildberries — Tariffs” добавлена в конец страницы `/app/project/[projectId]/marketplaces`.  
- В `Topbar` для superuser‑пользователей добавлен явный пункт меню `Admin: WB Tariffs`, ведущий на `/app/admin/marketplaces/wildberries/tariffs`, так что кнопку можно найти без ручного ввода URL.  
- Обновлён отчёт `docs/changes/SPRINT_WB_TARIFFS_ADMIN_UI.md`: зафиксированы нюансы task_id, поведения /status для `commission`, пути в интерфейсе (через Topbar и project‑страницу), условия видимости кнопок и сценарии ручной проверки; синтаксис и линтер‑проверки по изменённым файлам проходят успешно.
